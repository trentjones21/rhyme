# Web leftover

This folder is the merged portrait **web** campaign (PR 1). It is the design/content source the LÖVE port was written from.

It is **not** how you play Rhyme. Do not serve it. Do not wrap it. From the repo root:

```bash
love .
```
